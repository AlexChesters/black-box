name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: install poetry
        run: curl -sSL https://install.python-poetry.org | python3 -
        shell: bash